# requirements.txt
# Version: 2025-09-14 08:35 (Europe/Berlin)
# Zweck: Abhängigkeiten für Wissensdatenbank (Build & Sync)

# Markdown-Rendering für build_site.py
markdown>=3.6

# YAML-Frontmatter & JSON-LD
PyYAML>=6.0.1

# CSV/Sheets-Sync (statt gspread)
pandas>=2.2
requests>=2.32
