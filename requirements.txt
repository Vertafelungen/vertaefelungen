# =============================================================================
# requirements.txt
# Version: 2025-09-11 10:00 (Europe/Berlin)
# Zweck: Abhängigkeiten für sync-from-sheet.py und build_site.py
# =============================================================================

# Für build_site.py (Markdown-Render)
markdown>=3.6

# Für sync-from-sheet.py (Google Sheets + YAML)
gspread==5.12.0
oauth2client==4.1.3
PyYAML>=6.0.1
